import React from 'react';
import './Category.styles.scss';

const Category = ({match}) => {
    console.log(match);
    return(
        <div>
            CATEGORY PAGE
        </div>
    );
};

export default Category;